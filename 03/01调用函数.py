#绝对值
print(abs(-5))

#最大值
print(max(3, 4, 5, 99, -188, 4, 0))

#数据转换
# Python内置的常用函数还包括数据类型转换函数，比如int()函数可以把其他数据类型转换为整数：
# int() float() str() bool()

#函数名其实就是指向一个函数对象的引用，完全可以把函数名赋给一个变量，相当于给这个函数起了一个“别名”：
a = abs
print(a(-299))

a = 255;
a = int(a)
a = hex(a)
a = str(a)
print(a)
